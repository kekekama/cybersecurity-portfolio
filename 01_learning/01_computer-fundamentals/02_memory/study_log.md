# memory study log

## 26/03/2026

### Topics studied

-Ram

### What I learned

1) How computers store numbers
2) How computers know what actions to carry out
3) How computer execute a program

A computer writes numbers in binary or base two (0,1);
simple computers had 8-bit memory (the largest number is 255).

Storage: when computers remember stuff for long run;
Ram: Random access memory.

A program is a series of instruction;
in 16 bits, from 1 to 13 specify an address in memory, from 14 to 16 specify an action.

The computer begins at the top line in the memory, reading it into the processor.
Firstly the line is read and separated into an address+instruction;
the second part of the processor is the accumulator (a little register for temporary result).

Ram is the principal memory, it has data and instructions, it is volatile (if yout turn off the pc it free up space) and speedy.

The memory is composts of memory cell, every cell has an address.
Inside the cells there are bit
8 bit = 1 byte

